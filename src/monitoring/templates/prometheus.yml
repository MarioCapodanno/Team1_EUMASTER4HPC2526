global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'benchmark_framework'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['localhost:5000']  # Will be replaced with actual host:port
